{
  "name": "shows",
  "version": "1.0.0",
  "description": "This is an App that displays Animes ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "babel-node server.js",
    "build": "npm run server-install && npm run client && npm run build",
    "client": "cd client && npm run start",
    "server": "nodemon server --ignore client/",
    "client-install": "cd client && npm install",
    "server-install": "npm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ItamarTati/shows.git"
  },
  "author": "Itamar Tati",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ItamarTati/shows/issues"
  },
  "homepage": "https://github.com/ItamarTati/shows#readme",
  "dependencies": {
    "@vercel/node": "^2.9.13",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "helmet": "^6.0.1",
    "mongoose": "^7.0.2",
    "nodemon": "^2.0.21"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "@babel/node": "^7.10.1"
  }
}
